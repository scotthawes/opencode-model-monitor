# Runtime state generated by the monitor
state/

# Dependencies
node_modules/

# Logs
*.log

# User config (may contain local paths/secrets — do not commit)
config.json

# Subscriber list (may contain webhook URLs / env-var names — do not commit)
subscribers.json
